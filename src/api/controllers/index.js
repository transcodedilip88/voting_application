const authController = require('./auth.controller')
const candidateController = require('./candidate.controller')
const voteController = require('./voting.controlller')
const userController = require('./user.controller')

module.exports ={
    authController,
    candidateController,
    voteController,
    userController
}